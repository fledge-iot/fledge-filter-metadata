=====================================================
Build and Run test cases
=====================================================

To build Fledge "metadata" C++ filter plugin unit test cases:

.. code-block:: console

  $ mkdir build
  $ cd build
  $ cmake ..
  $ make
  $ ./RunTests

